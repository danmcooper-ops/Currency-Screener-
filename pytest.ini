[pytest]
testpaths = tests
# Live tests hit four third-party APIs and are excluded by default so the
# default suite stays offline, fast and deterministic. Run them with:
#     pytest -m live
addopts = -m "not live"
markers =
    live: hits real upstream endpoints (network required)
